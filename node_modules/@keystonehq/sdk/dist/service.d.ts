import { Play, Read, SDK } from "./types";
export declare const setupSdk: (r: Read, p: Play, status: boolean) => void;
declare const sdk: {
    bootstrap: () => void;
    getSdk: () => Promise<SDK>;
};
export default sdk;
