import React from 'react';
interface ITorchProps {
    scanning: boolean;
    switchTorch?: (value: boolean) => void;
}
export default function Torch(props: ITorchProps): React.JSX.Element | null;
export {};
